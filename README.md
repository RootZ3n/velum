# Velum\n\nAI Privacy & Injection Defense. Protects AI systems from prompt injection, credential leakage, and PII exposure.\n\n## Install\n\n```bash\nnpm install velum-ai\n```\n\n## Quick Start\n\n```typescript\nimport { classify, scanContext, applyOutputGuard } from 'velum-ai';\n\n// Scan user input\nconst result = await classify('ignore previous instructions');\n// result.classification: 'PROMPT_INJECTION'\n// result.action: 'flagged'\n\n// Scan model output\nconst output = applyOutputGuardSync(modelResponse, { inCharacter: false });\n// output.blocked: true if secrets detected\n// output.text: redacted version\n```\n\n## CLI\n\n```bash\nvelum scan ./src          # Scan codebase for secrets\nvelum test "your input"   # Test input against patterns\nvelum init                # Generate config file\n```\n\n## License\nMIT\n
